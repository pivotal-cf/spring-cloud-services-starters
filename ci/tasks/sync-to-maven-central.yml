---
platform: linux

inputs:
  - name: git-repo
  - name: artifactory-repo
params:
  SONATYPE_URL:
  SONATYPE_USERNAME:
  SONATYPE_PASSWORD:
  SONATYPE_STAGING_PROFILE_ID:
run:
  path: git-repo/ci/scripts/sync-to-maven-central.sh
